# Задание 3. Полиморфизм.

## Цель работы.

Совершенствование навыков объектно-ориентированного программирования, использования механизма наследования.
Изучение свойства полиморфизма, механизмов раннего и позднего связывания.

## Постановка задачи.

Дополнить класс треугольник из предыдущей лабораторной работы виртуальным методом Show(). Наделив его следующим
функционалом, отображение на экране полной информации об объекте, значение сторон треугольника и известного угла,
периметра и площади. В классе пирамида метод Show() должен быть переопределён, с расширением функционала, кроме
перечисленного, необходимо вывести на экран значение высоты и объёма пирамиды.
Объявить указатель на объект класса треугольник и указатель на объект класса пирамида. Настроить объявленные указатели
на объекты соответствующих классов.

Вызвать метод Show() через объявленные указатели.
Перенастроить указатели на объекты не соответствующих классов. Закомментировать строчку дающую ошибку, объяснить эту
ошибку в комментариях.

Снова вызвать метод Show(). Объяснить полученный результат.

### Контрольные вопросы

| Вопрос                                                                                                          | Ответ                                                                                                                                                                                                                                                                               |
|-----------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Какую функцию называют виртуальной?                                                                             | Виртуальной функцией называют методы, помеченные ключевым словом virtual. Данные методы, в отличие от абстрактных должны иметь реализацию в родительском классе (где они объявлены), но могут быть перезаписаны в классе - наследнике                                               |
| Поясните механизм раннего и позднего связывания.                                                                | Есть два вида связывания: ранее – тип объекта определяется на этапе компиляции и позднее – тип объекта определяется на этапе выполнения.                                                                                                                                            |
| Что такое таблица виртуальных методов?                                                                          | Таблица виртуальных методов представляет собой таблицу, создаваемую компилятором. Данная таблица хранит адреса всех виртуальных методов класса. На каждый класс с виртуальными методами создается своя таблица, для каждого экземпляра класса хранится указатель на данную таблицу. |
| Каким образом осуществляется связь с таблицей виртуальных методов?                                              | В момент вызова виртуального метода  берется указатель на виртуальную таблицу, по которому производится поиск самой таблицы. Внутри таблицы находится адрес нужного метода и происходит вызов метода по данному адресу.                                                             |
| Каким образом определяется адрес нужного метода в таблице виртуальных методов?                                  | Адрес (индекс) метода определяется порядком объявления виртуальных методов в классе                                                                                                                                                                                                 |
| В чём преимущества позднего связывания?                                                                         | Позволяет вызывать методы ребенка (экземпляра) даже если переменная имеет тип родителя                                                                                                                                                                                              |
| В чём недостатки позднего связывания?                                                                           | Как и все runtime вещи – скорость выполнения, дополнительные выделения памяти на виртуальную таблицу                                                                                                                                                                                |
| В каком случае виртуальный метод родительского класса останется виртуальным в классе наследнике?                | Если ребенок не переопределил данный метод                                                                                                                                                                                                                                          |
| Какие методы нет смысла делать виртуальными?                                                                    | Методы, которые не планируется переопределять в наследниках                                                                                                                                                                                                                         |
| При вызове виртуального метода через указатель, на объект, метод будет вызван по типу указателя или указуемого? | Виртуальные методы вызываются по типу экземпляра, а не типу переменной, в которую он записан                                                                                                                                                                                        |


### Задания для самостоятельной работы

1. Описать иерархию классов: учащийся, ученик, студент, работающий студент, метод вывода информации об объекте сделать полиморфным.
2. На основе класса Shape (фигура) описать класс Ellipsoid (эллипсоид). Эллипсоид будет характеризоваться данными х, у, z (наследуются от Shape) и a,b,с - полуоси. В классе Ellipsoid переопределить функцию для расчета объема. Объем эллипсоида вычисляется по формуле V = 4nabc/3.
3. Создать абстрактный базовый класс с виртуальной функцией Площадь. Создать произвольные классы Прямоугольник, Круг, Прямоугольный треугольник, Трапеция со своими функциями площади. Для проверки определить массив ссылок на абстрактный класс, которым присваивается адреса различных объектов.
4. Разработать иерархию классов. В иерархии должно быть не менее двух уровней и не менее четырех классов (вместе с базовым классом). 
   - Каждый класс - потомок должен иметь хотя бы один собственный атрибут, и как минимум один собственный метод.
   - Предусмотреть не менее одного виртуального метода, например, метод вывода информации об объекте на экран. 
   - В каждом классе - потомке должен быть описан собственный конструктор. 
   - В основной программе надо привести небольшой демонстрационный пример, показывающий возможности работы с классами - потомками: 
     - создание экземпляра класса - потомка
     - доступ к атрибутам и методов класса - предка
     - порядок вызова конструкторов
5. Создать абстрактный базовый класс объектов на декартовой плоскости CShape (фигура). Разработать и реализовать иерархию классов конкретных фигур: CPoint, CCircle и т.д., не менее четырёх классов наследников. При этом: во всех классах должны быть реализованы:
   - Square(), вычисляющий и возвращающий площадь
   - Show(), который отображает на экране информацию в текстовом виде о данном экземпляре (не менее трех характеристик; у точки есть имя и координаты, у окружности ещё есть радиус и площадь, у ломаной - длина и так далее.




